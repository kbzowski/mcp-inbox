name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['24.x']
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run format:check
      - run: npm run test
      - run: npm run build

  # Integration job — activated in Phase 3 when real tests + GreenMail bootstrap land.
  # Intentionally left commented so CI stays green during scaffolding phases.
  #
  # integration:
  #   runs-on: ubuntu-latest
  #   needs: check
  #   services:
  #     greenmail:
  #       image: greenmail/standalone:2.1.0
  #       ports:
  #         - 3143:3143 # IMAP
  #         - 3025:3025 # SMTP
  #       env:
  #         GREENMAIL_OPTS: '-Dgreenmail.setup.test.all -Dgreenmail.auth.disabled -Dgreenmail.users=test:test@localhost'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '24.x'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run test:integration
  #       env:
  #         GREENMAIL_IMAP_PORT: 3143
  #         GREENMAIL_SMTP_PORT: 3025
